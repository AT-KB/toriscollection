# 最初に読む — 引継ぎの要点(2026-08-21 時点)

引継ぎが11本・約170KB になったので、**大事なところだけ**をここに集めた。
詳細は各文書へ。迷ったらこの1枚 →『2026-08-18 作業方針』→ 該当日付、の順。

---

## 1. いま何が動いているか

| | 実体 | 状態 |
|---|---|---|
| **製品** | `toris_app/`(Flutter)+ `toris_core/`(共有ロジック) | **Play で公開中 versionCode 8 / 2.0.0** |
| **次の版** | `release/toris-collection-2.0.2-10.aab` | **未アップロード**(ラジオ修正・アイコン・広告) |
| **ロジックの正** | `toris_collection/`(Streamlit) | **消せない**(後述) |
| **LP** | 原稿 `landing/` → 公開は別リポジトリ | **公開済み** torriscollection.com |
| **広告** | AdMob 実装済み・本番ユニット | **no-fill**(ユニットが新しく未配信) |

**パッケージ名**: release = `com.toriscollection.app`(Play 公開済みと同じ) /
debug = `.dev` を足して別物(開発ビルドが Play 版を潰さないため)。
**署名鍵**: `C:\Users\kubok\TorisCollection-ReleaseKeys\`。指紋
`E2:9D:2C:40:...:6C:45:F7`。**applicationId と鍵を変えたらユーザーのデータが消える。**

---

## 2. 進め方の背骨(ここだけは崩さない)

出典『2026-08-18 作業方針』。**この5つが一番よく効いた。**

1. **推測で書かない。一次情報を読む。** ID も文言も数値も、`data.py` /
   `i18n.py` / 実装と突き合わせてから使う。勘で書いて実際に外したことが何度もある。
2. **答えは Python から取る。** 手で期待値を書かない。`tools/logic_fixtures.py` が
   Python を回して JSON に落とし、Dart がそれと一致するかを見る。
3. **テストは「落ちること」を確かめてから信じる。** 新しいテストを足したら、
   わざとコードを壊して落ちるのを見る。**規則(監査)も同じ**
   — 2026-08-21 に緩すぎる正規表現と文字列一致の誤検出で2回やらかした。
4. **監査をすり抜けた不具合が出たら、不具合を直して終わりにしない。**
   同じ**種類**を次に捕まえる規則を足す。
5. **交渉不能の5原則**(受動的 / 罰しない / 声と癒しは無料 / 生態に誠実 /
   かわいさ優先)は**宣伝物にも効く**。映っていないものを宣伝で言わない。

---

## 3. 何度も人を止めた罠(全部実際に踏んだ)

### 🔴 LP を直しても本番に出ない
`torriscollection.com` は **`AT-KB/aviation-note-site`** の GitHub Pages。
`landing/` は原稿。公開は向こうへ `index.html` をコピーして push。
**`index.html` 以外は触らない**(CNAME と app-ads.txt が同居している)。
→ §7.5(2026-08-21)、2026-08-10

### 🔴 Play は「アップロードしただけ」では配信されない
アップロード後、**リリースを作って「公開を開始」まで押す**必要がある。
`公開の概要` に保留が出ていたら、そこで送信するまで永久に「未公開」。
versionCode **7 はこれで丸一日止まっていた**(状態が「非アクティブ」)。
→ 2026-08-21

### 🔴 release ビルドでしか出ない不具合がある
- **R8 が Room の生成クラスを消して起動即クラッシュ**(広告が WorkManager を
  連れてくる)。`proguard-rules.pro` で keep。**debug では再現しない。**
- **DEBUG の赤帯**はストア用スクショに写る。撮影は必ず release で。
→ 2026-08-21

### 🔴 「移してあるのに誰も呼んでいない」
台帳は**宣言があるか**しか見ない。中身が違っても「済み」と出る。
実際、**「移した」100件のうち35件が一度も突き合わせられていなかった**。
ラジオが会っていない鳥まで鳴らしていたのはこれ。
→ 対策は §6.5(未検証のラチェット / ゼロ状態テスト / 主張のテスト化)

### 🔴 台帳は Python の**公開関数**しか数えない
UI 関数の**中に直接書かれた条件**は載りようがない。外部リンクの移植漏れ
(2026-08-19)も、ラジオの絞り込み(2026-08-21)も、どちらもこれ。
**移植で条件を見つけたら、関数に切り出して突き合わせ対象にする。**

### 🔴 OneDrive 配下で release ビルドが落ちる
`build/.cxx` のロック。消せば通る。

---

## 4. 実機を触るときの作法

出典『2026-08-18 作業方針』§6 + 実地で足したもの。

1. **CEO の設定を変えない。** 目覚ましの時刻・鳥、庭の植物、土地。
   検証は**別パッケージ**(`.dev` / 旧 `toris_app`)でやる。
2. **`pm clear` を使わない。** 権限(通知)まで消える。
3. **スクショは縮小してから読む。** 撮る枚数も絞る(トークンを使いすぎない)。
4. **座標は端末ごとに違う。** Pixel 6a=1080x2400 / 9a=1080x2424。
   タブを叩く前に `wm size` を見る。
5. **別ユーザープロファイルに古い版が残っていると Play からインストールできない**
   ("another user has already installed an incompatible version")。
   `pm list users` と `dumpsys package <pkg>` で確認 → `pm uninstall --user N`。

---

## 5. 消せないもの・変えてはいけないもの

| | なぜ |
|---|---|
| `toris_collection/`(Streamlit) | `migration_audit.py` が AST で読み、`logic_fixtures.py` が import する。**消すと監査と突き合わせが丸ごと死ぬ** |
| `toris_collection/designbird/` | 動画ビルド4本と `xc_marketing_pull.py` が参照 |
| `flutter_spike/README.md` | `toris_app/README.md` が参照(Gradle が落ちる理由) |
| applicationId / 署名鍵 | 変えるとユーザーのデータが消え、Play 更新も通らない |
| `app-ads.txt` / `CNAME`(公開リポジトリ側) | AdMob の確認とドメインが飛ぶ |

---

## 5.5 部署(2026-08-21 に監査を足した)

| | 役 | 定義 |
|---|---|---|
| 企画部 `kikaku` | 体験設計・仕様・数値バランス。コードは書かない | `.claude/agents/kikaku.md` |
| SNS部 `sns` | 発信・ASO・コラボ文面。コードに触らない | `.claude/agents/sns.md` |
| 開発部 `dev` | 実装・データ・テスト・デプロイ。**コードを触る唯一の部署** | `.claude/agents/dev.md` |
| **監査 `kansa`** | **部署ではなく横断機能。** 成果物を作らず、**言っていることとあるものが合っているか**だけを見る | `.claude/agents/kansa.md` |

**監査は開発部の道具ではなくなった**(CEO 2026-08-21「監査はすべてに入り込んで
チェックすべき」)。コードだけでなく、**ストア掲載文・LP・動画字幕の言い切り**も対象。
規約は `docs/team/06_監査.md`、**外に出る前に通す**(`05_運用ルール.md` §1.5)。

きっかけは、掲載文の "A radio made only of birds you've met" が
**実装と食い違ったまま数週間配信されていた**こと。掲載文は SNS部、実装は開発部、
**その間を見る人がいなかった。**

## 6. どの文書に何が書いてあるか

| 文書 | 中身 |
|---|---|
| **2026-08-18 作業方針** | **進め方の背骨**。まずこれ |
| 2026-08-21 | Play 更新 / 広告移植 / 監査の作り直し / 整理 / LP |
| 2026-08-18 到来と目覚まし | 「鳥が来ない」の正体(前面復帰で庭が進んでいなかった) |
| 2026-08-15 | Flutter 移植の本体(最大・38KB) |
| 2026-08-13 | 移植の入り口、Flutter 環境 |
| 2026-08-10 | **LP の配信元** / AdMob の2つの関門 |
| 2026-08-08 | **TikTok 診断**(8秒型・冒頭1秒) |
| 2026-08-02 / 07-14 / 06 / I18N | それ以前 |
| `docs/team/00_共通サマリ.md` | **交渉不能の5原則**。全部の判断の親 |
| `docs/team/proposals/` | 個別の設計(広告・目覚まし・Flutter移行の判断 など) |
| **`docs/team/06_監査.md`** | **監査の規約。3つの層・規則の書き方・主張台帳** |
| `docs/team/02_SNS部.md` | マーケ方針。**チャネル(TikTok)・課金の考え方・動画の作法** |

---

## 7. いま残っている宿題(優先順)

1. **2.0.2+10 を Play に上げる** — ラジオ修正が入っている(**会っていない鳥が
   鳴いていた**のは掲載文にも反する)。広告を出すので
   「広告が含まれる」+ データセーフティの申告が要る
2. **目覚ましの穴** — 強制終了/再起動で登録が落ちる。一度鳴ったら終わり。
   **毎日鳴らすか単発かは CEO 判断**(v6 も同じなので悪化はしていない)
3. **TikTok** — バッチ1の完全視聴率がまだ未取得。**これを読むまで課金しない**
   (100再生で0DLは期待値どおり。→ 2026-08-21 の事例調査)
4. **ドット絵** 37種中12種が未着手。**Song Sparrow は目覚ましのコーラスに出る**ので優先
5. **未検証の移植 35件** を減らす(ラチェットが増加だけは止めている)
6. `.git` が 391MB。減らすには履歴の書き換えが要る(**指示待ち**)
