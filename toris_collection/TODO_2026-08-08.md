# ToDo 一覧（2026-08-08 時点）

クローズドテストの審査通過を受けて、残っている作業を1枚にまとめたもの。
**◆ = CEOの手作業 / ◇ = 開発側で進められる作業**

---

## 0. Google Play ポリシー（2026-07-15 のお知らせ）への適合状況

| 項目 | 該当 | 状態 |
|---|---|---|
| **対象APIレベル（期限 2026-08-31）** | 該当 | ✅ **充足**。`variables.gradle` の `targetSdkVersion = 36`（compileSdk も36）。**コード変更不要** |
| Android デベロッパー確認 / パッケージ名登録 | 該当 | ✅ **完了済み**。Play Console で `com.toriscollection.app` が「登録済み（2026年7月16日）」。**対応不要** |
| 匿名チャット / ランダムチャット | 対象外 | チャット機能なし。「みんなの庭」も MVP で非表示、ユーザー間の投稿・メッセージ機能ゼロ |
| SMS・通話履歴（READ_CALL_LOG） | 対象外 | 権限は `INTERNET` と `AD_ID` のみ（他は SDK 由来: NETWORK_STATE / WAKE_LOCK / FOREGROUND_SERVICE / AdServices） |
| データセーフティの位置情報開示 | 対象外 | 位置情報の権限を一切使わない |
| サードパーティ AI 統合のユーザーデータ | 対象外 | アプリに LLM / AI 統合なし |
| EWA・個人ローン | 対象外 | |

**→ このお知らせを理由に直すコードはありません。**

---

## 1. リリース（審査通過で今いちばん動く場所）

### ◇ 1-1. AdMob を本番 App ID に差し替える【公開前必須】
`android_app/android/app/src/main/res/values/strings.xml:13` が
**Google公式のテスト用 App ID**（`ca-app-pub-3940256099942544~3347511713`）のまま。
このまま本番に出すと AdMob アカウントに紐づかず、**広告収益がゼロ**（表示もテスト広告）。
◆ CEO が AdMob で本番 App ID を取得 → ◇ 差し替え。

### ◇ 1-2. versionCode 4 でビルドし直してクローズドに再アップ【確定した問題】
- `versionCode 3` が入ったのは **2026-07-14**（コミット `39b63b2`）。
- 起動スプラッシュ／アイコンの修正は **2026-08-02**（`0a50cc3` / `ae9caed`）。
- **その後 versionCode は上がっていない。**

→ **いまテスターに届いているビルドには、スプラッシュ修正が入っていない。**
「起動画面が出ない」問題は、リポジトリでは直っているが端末には届いていない状態。
versionCode を 4 に上げて AAB を再ビルドし、クローズドテストに上げ直す。

### ◆ 1-3. Console で2点の提出状況を確認
- **コンテンツレーティング**（レーティングなしのアプリは配信不可になった）
- **データセーフティ**（`AD_ID` 権限があるので**広告IDの収集申告**が要る）

`ROADMAP_GOOGLE_PLAY.md` L94 のチェック項目が未チェックのままなので、
実際は提出済みでも記録が追いついていない可能性が高い。確認して記録を合わせる。

---

## 2. アプリ本体

### ◇ 2-1. 図鑑プロフィールを実機で確認 → PR・merge
本日実装（好きなもの / 好きな場所 / こわいもの）。ブランチ `feat/dex-profile-predators` に
2コミット、**未 push**。pytest 204 passed / AppTest 起動時例外0 / 生成HTMLのブラウザ確認は
済んでいるが、**実機の図鑑での目視は未実施**（端末未接続のため）。

### ◆ 2-2. 肉食鳥（猛禽）拡張の承認 — §9 の3点
`proposals/2026-08-02_肉食鳥拡張_企画提案.md`
① 罰しない解釈（可逆な到来率低下のみ・図鑑/会った日数/声は不変）でよいか
② Kyoto は Charlotte 先行でよいか ③ 数値は実測で詰める前提でよいか

※ 図鑑の「こわいもの」は **この承認を待たずに実装済み**（GloBI の preyedUponBy を
各鳥から直接引いたため、engine もデータ構造も変えずに成立した）。
この提案は「猛禽を**収集対象の鳥として追加する**」という別の話として残っている。

### ◆ 2-3. Sim タブの整合
`01_企画部.md` は「シミュ・ラボ＝棚上げ」だが、実機にはSimタブが存在する。
隠す / 整える / 方針変更 のいずれかをCEO判断。

---

## 3. SNS（TikTok 立て直し）

診断と仕様は `marketing/phase-1/SNS部/2026-08-08_TikTok立て直し_診断と仕様変更.md`。
**原因は本数不足ではなく、冒頭3.2秒が静止画に見えること**（現物のフレームで確認済み）。

### ◆ 3-1. 仕様変更の承認（4点）
① 尺 15秒→**8秒ループ**・主役を**0.3秒から**出す（型自体は維持）
② **日本語版を主軸**にしてよいか（英語ファーストの既定方針の変更）
③ 1日2本×3週間・**TikTok集中**の運用（毎日の投稿操作がCEOの手作業）
④ 動画から **"link in bio" を外す**

### ◇ 3-2. 承認後の作業
`build_reveal.py` に8秒型を追加 → 日本語フォント対応 → 図鑑プロフィールの
「こわいもの」を問いに使って W1 分の14本を書き出す（25種×3項目＝75本分のネタがある）。

### 済み（本日）
- 商用利用可（CC0/BY/BY-SA）の音源を **+12種**取得（`tools/xc_marketing_pull.py`）。
  スズメ・カワセミ・ツバメ・カケス・エナガが入り、**京都側の動画が作れるようになった**。
  ※ `radio_src` は .gitignore 管理外なので、このツールが唯一の再現手段。
- `build_reveal.py` を VARIANTS 化（種を差し替えるだけで増産できる）。

---

## 4. 片付け

### ◇ 4-1. 未コミットファイルの仕分け
`tools/video_build/build_quiz.py` / `_quiz/` / `post13_quiz_robin_1080x1920.mp4` /
`raw/fgchk` `raw/meetchk` `raw/quizchk` / `ヘッダー.png` /
`tiktok...txt`（ドメイン認証ファイル2つ）が untracked のまま。
コミットするか捨てるかを決める。

---

## 進める順番（推奨）

1. **1-2（versionCode 4 で再アップ）** — テスターに修正が届いていない状態を解消するのが最優先
2. **1-3（Console 2点の確認）** — CEOの手作業。1-2 のアップロードついでに見られる
3. **2-1（図鑑プロフィールの実機確認 → merge）** — 端末を繋いだタイミングで 1-2 と一緒に
4. **3-1（SNS仕様の承認）** — 承認さえ出れば書き出しは自動で回せる
5. **1-1（AdMob本番ID）** — 本番公開の直前でよい（クローズドの間は急がない）
