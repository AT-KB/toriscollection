<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!--
          AdMob アプリID(@capacitor-community/admob 導入、2026-07-08)。
          今はGoogle公式のテスト用App ID(ca-app-pub-3940256099942544~3347511713)を
          使用中。本番配信前に、CEOがAdMobで取得した実際のApp IDに
          strings.xml の admob_app_id を差し替えること(このファイルは変更不要)。
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_app_id"/>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!--
      Google Mobile Ads SDK(AdMob)の広告ID取得に必要。
      targetSdkVersion 33以降でこのSDKを使う場合、この宣言が無いと
      SDK初期化・広告ID取得が正常に完了しないことがある(2026-07-10 実機で
      リワード広告が「白画面→即終了」する不具合の調査で発覚)。
    -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
</manifest>
