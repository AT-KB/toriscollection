name: toris_core
description: "Toris Collection のロジック(UI を持たない部分)。Streamlit からの移行用。"
version: 0.1.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.2

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
